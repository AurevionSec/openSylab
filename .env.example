# Copy this file to `.env` (untracked) and set strong random values.
# Each secret must be at least 32 characters. Generate with:
#   openssl rand -hex 32
OPENSYLAB_JWT_SECRET=
OPENSYLAB_AUDIT_HMAC_KEY=
